<Window x:Class="CourseProject.Lab5.Lab5Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Лабораторная работа 5" Height="600" Width="800"
        Background="#FFF0F0F0">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#FF6200EE"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="50"/>
            <Setter Property="BorderBrush" Value="#FF6200EE"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Лабораторная работа 5" FontSize="20" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="20"/>
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <WrapPanel HorizontalAlignment="Center">
                <Button Content="Загрузить файл" Click="LoadFileButton_Click"/>
                <Button x:Name="CompressButton" Content="Сжать" Click="CompressButton_Click"/>
                <Button x:Name="RestoreButton" Content="Восстановить" Click="RestoreButton_Click" Visibility="Collapsed"/>
                <Button x:Name="SaveImageButton" Content="Сохранить изображение" Click="SaveImageButton_Click"/>
            </WrapPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock x:Name="SliderLabel" Text="Степень сжатия:" VerticalAlignment="Center" Margin="10"/>
                <Slider x:Name="CompressionSlider" Minimum="1" Maximum="100" Value="50" Width="200" Margin="10"/>
                <TextBox x:Name="CompressionTextBox" Text="{Binding ElementName=CompressionSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <ProgressBar x:Name="ProgressBar" Width="400" Height="20" Margin="10" Visibility="Collapsed" IsIndeterminate="True"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Image x:Name="OriginalImage" Width="300" Height="300" Margin="10"/>
                <Image x:Name="CompressedImage" Width="300" Height="300" Margin="10"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>